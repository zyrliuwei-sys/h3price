# H3 review changes — 2026-09-06

The user's later instruction overrides B1: preserve the original homepage composer, its options, and its submit behavior. A regular-price card and two semantic tool links are placed in the lower pricing area, before the subscription plans, per the user’s placement correction.

| Items | Result                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A1    | Real auth/database handlers passed registration, verification, unverified-login rejection, login, repeated session requests, and logout using an in-process mail sink. No real email was sent. Current development settings have no email provider and verification is disabled; actual delivery remains to be configured and tested. Verification enabled without a provider no longer silently permits unverified access. |
| A2    | Email/password registration, unchecked required privacy consent, server-side email-signup consent validation, and revised English/Chinese privacy policies. OAuth signup UI also requires the checkbox.                                                                                                                                                                                                                     |
| A3    | Anonymous alert-request API and calculator form, separate consent, durable email/consent-version/timestamp storage, duplicate suppression, validation, origin check, and basic process-local throttling. This collects requests; automatic price monitoring and notification delivery are not implemented. Withdrawal currently uses the privacy contact.                                                                   |
| A4    | All seven auth routes emit noindex/nofollow, titles, and descriptions in source HTML.                                                                                                                                                                                                                                                                                                                                       |
| A5    | All four public pages returned identical SSR main text with and without a valid session cookie.                                                                                                                                                                                                                                                                                                                             |
| B1/B2 | Original composer preserved per latest instruction. Added regular-price card in the lower pricing area and Links to calculator/prompt generator; converted the separate marketing CTA to Link. The original composer's Generate control retains its existing behavior.                                                                                                                                                      |
| C1–C4 | Three-card Tools section after pricing, contextual links on prompt/comparison pages, four-page footer navigation, configured brand and copyright.                                                                                                                                                                                                                                                                           |
| D1    | Four pages use a 1200×630 PNG for OG/Twitter; PNG response and dimensions verified.                                                                                                                                                                                                                                                                                                                                         |
| D2    | Not changed; awaiting the user's confirmation about removing zh hreflang.                                                                                                                                                                                                                                                                                                                                                   |
| D3    | Canonical origin uses VITE_SITE_URL, independent of local authentication origin. The production-domain literal is confined to src/config and the environment example.                                                                                                                                                                                                                                                       |
| E1    | No DNS changes or deployment performed. The production-domain request timed out during this check; production 200 is not verified.                                                                                                                                                                                                                                                                                          |
| E2    | Sitemap derives its four entries from the shared SEO page registry. Local robots/sitemap return 200 and point to the configured production origin. Production availability depends on E1 and deployment.                                                                                                                                                                                                                    |

## Validation

- `pnpm db:push`: applied the new price_alert table using .env.development. PostgreSQL, SQLite, and MySQL templates are updated.
- `pnpm build`: passed after the final code changes.
- `pnpm exec tsx scripts/with-env.ts pnpm exec tsx scripts/check-h3-auth.ts`: passed.
- `pnpm exec tsx scripts/with-env.ts pnpm exec tsx scripts/check-h3-pages.ts`: passed.
- Checks expect the local dev server on port 3001; override with H3_CHECK_ORIGIN. Both scripts create uniquely named test records and remove their own records afterward. Run against a development database.
- Browser inspection confirmed the original composer, price card, signup consent, calculator input response, and alert form.
- A separate TypeScript check reported pre-existing errors in the video studio, fal-h3-max service, and evolink route; the production build succeeds. These unrelated files were not changed for this task.

## Remaining environment work

Configure an email provider and enable email verification in /admin/settings, then verify delivery to an owned inbox. Before deploying to a separate production database, generate and review the price_alert migration, then apply the reviewed migration.

In Vercel Project Settings → Domains, add the production domain and copy the exact DNS record values shown for that project into Cloudflare DNS. Verify the domain status and production HTTP responses afterward. Do not guess an IP or CNAME from another project. See [Vercel domain setup](https://vercel.com/docs/domains/working-with-domains/add-a-domain) and [Cloudflare 522 troubleshooting](https://developers.cloudflare.com/support/troubleshooting/http-status-codes/cloudflare-5xx-errors/error-522/).
